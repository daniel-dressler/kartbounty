diff a/Standard.h b/Standard.h	(rejected hunks)
@@ -22,7 +22,7 @@
 #include <Windows.h>
 
 #include <crtdbg.h>
-#define DEBUGOUT( str, ... ) { char out[4096]; sprintf_s( out, 4096, str, ##__VA_ARGS__ ); OutputDebugString( out ); }
+#define DEBUGOUT( str, ... ) { char out[99999]; sprintf_s( out, 99999, str, ##__VA_ARGS__ ); OutputDebugString( out ); }
 //#define DEBUGOUT( str, ... ) {}
 
 #ifdef _DEBUG
